services:
  - type: web
    name: upshiftrx-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m uvicorn src.api:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    envVars:
      - key: PUBMED_EMAIL
        value: contact@upshiftrx.ai